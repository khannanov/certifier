import React, { PropTypes } from 'react'

const QuestionForm = (props) => {
  return (
    <form>
      this is question form
    </form>
  )
}

QuestionForm.propTypes = {}
QuestionForm.defaultProps = {}

export default QuestionForm