import React, { Component, PropTypes } from 'react';

class Header extends Component {
  render() {
    return (
      <header className="intro-header">
        will be an awesome header
      </header>
    )
  }
}

Header.propTypes = {};

export default Header;